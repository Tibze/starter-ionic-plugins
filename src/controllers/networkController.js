'use strict';

/**
 * @ngdoc function
 * @name IonicPlayground.controller:SettingsController
 * @description
 * # SettingsController
 */
angular.module('ionicPluginPlayground')
  .controller('NetworkController', function($rootScope,$scope) { 

    // $scope.networkAvailable = $rootScope.networkAvailable;

    // $rootScope.$watch('networkAvailable', function() {
    //    console.log($rootScope.platformReady);
    //    if ($rootScope.networkAvailable) {
    //         $scope.networkAvailable = true;
    //     }
    //    else {
    //         $scope.networkAvailable = false;            
    //     }
    // });  	     

  });
